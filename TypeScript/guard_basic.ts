function processEx(value: string | number){
  // to change large charactor of argument 'value'
  // return value.toUpperCase();

  if(typeof value === 'string'){
    return value.toUpperCase();
  }
}
