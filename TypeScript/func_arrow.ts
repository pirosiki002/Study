// let triangle =
// (base:number, height:number):number =>{
//   return base * height / 2;
// };

let triangle =
(base:number, height:number):number => base * height / 2;

console.log(triangle(20, 5)); // result:50